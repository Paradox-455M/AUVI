# Copy this to .env and fill in the values.
# .env is gitignored — never commit real secrets.

# Required — generate a long random string, e.g.:
#   openssl rand -hex 32
JWT_SECRET=5754e7247dd277f4bf98f264e95c03307b0fe0319fd37ba34a04cbd9932c26e1

# Optional (these are the defaults)
DATABASE_URL=postgres://localhost:5432/auvi?sslmode=disable
PORT=8080
UPLOAD_DIR=./uploads
MAX_FILE_MB=100
